

export function isLoggedIn(cookie) {
    // parse cookie and get token from it https://www.npmjs.com/package/cookie
    // decode JWT token  https://www.npmjs.com/package/jsonwebtoken
    // check token scopes 
    // if all ok then true
    return true;
}